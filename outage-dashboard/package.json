{
  "name": "outage-dashboard-mockup",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained, buildless Spectrum + Cox post-merger outage dashboard mockup. The app opens directly via file:// with no build step; this package.json exists only for the test tooling.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.23.1",
    "jsdom": "^29.1.1",
    "vitest": "^2.1.8"
  }
}
